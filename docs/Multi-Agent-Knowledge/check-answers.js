window.CHECK_ANSWERS = {
  "01-全景与定义.md": [
    `普通模型调用通常是一次输入到一次输出；Agent 在模型外增加状态、工具、决策循环和停止条件；多智能体系统则让多个具有不同职责或权限的 Agent 通过消息、共享环境和协调机制共同完成任务。`,
    `支持采用多 Agent 的证据包括：任务能拆成相对独立的专家子任务，以及结果需要独立复核或权限隔离。反对证据包括：任务很短且步骤高度耦合，以及通信、重复上下文和评审成本可能超过分工收益。`,
    `任务是 <code>task = Message(..., "task", ...)</code>；消息契约是 <code>Message</code> 的 sender、receiver、kind、content；角色是 agents 表中的 researcher 与 critic；评审发生在 supervisor 创建 kind 为 review 的消息并交给 critic，结果保存在 <code>critic_result</code>。`
  ],
  "02-多智能体基础理论.md": [
    `闭环是：Agent 根据当前 observation 和内部状态选择 action，action 读取或改变 environment，环境产生新的 observation，Agent 更新状态后再次决策；任务完成或触发限制时由运行时停止。`,
    `每个 Agent 只能看到局部消息、局部工具结果或有限权限下的环境，因此无法自然拥有全局事实。证据卡或共享黑板把结论、来源、版本和置信度显式化，让其他 Agent 能复用和质疑，而不必相信一段无来源转述。`,
    `当投票者共享同一基础模型、提示模板、训练偏差或错误证据时，错误是相关的而不是独立的。此时增加同质票数只会重复共同错误；应先增加证据和推理路径多样性，再用测试或外部验证裁决。`
  ],
  "03-角色与团队设计.md": [
    `Developer 的职责可以是实现已批准需求，非目标是不自行修改业务范围，工具是可写代码工作区，输出是补丁和测试结果；Reviewer 的职责是发现缺陷，非目标是直接改写实现，工具为只读仓库和测试，输出是带证据的 review。`,
    `逐行检查能力矩阵：没有任何角色拥有某项必要能力表示角色缺位；多个角色同时拥有高风险写权限但没有所有权规则表示权限冲突；角色拥有完成职责所不需要的工具表示权限过宽。`,
    `Reviewer 发送结构化驳回，包含 artifact_id、版本、问题位置、严重级别、证据和验收条件；路由器把它交回原 Developer，Developer 基于同一产物创建新版本并附修复说明，随后再由 Reviewer 复核，旧版本保持可追踪。`
  ],
  "04-任务分解与规划.md": [
    `例如“发布技术报告”可拆为：Researcher 收集证据，依赖为空，验收为至少三条可定位来源；Writer 根据证据写初稿，依赖 Researcher，验收为章节完整；Reviewer 检查事实与引用，依赖初稿，验收为高风险问题清零。每个节点都应同时写 owner、dependencies 和 acceptance criteria。`,
    `把任务表示为 DAG 后，所有依赖已经完成且入度变为零的节点可以并行。共享同一可变资源、存在权限冲突或会产生相互覆盖写入的节点，即使图上无依赖，也需要锁、隔离工作区或显式串行化。`,
    `假设失效时回退到依赖该假设的最早节点并重算；工具失败时按可重试性选择重试、替代工具或人工升级；预算耗尽时停止低价值分支、缩小验收范围并请求主管确认，不能静默降低质量。`,
    `ToT 用树组织相对独立的候选路线，适合分叉、评分和回退；GoT 用一般图组织思路，允许分叉、合并、复用与反馈；任务 DAG 描述的是可执行子任务及其先后依赖，不是模型内部候选思路。`
  ],
  "05-协作拓扑.md": [
    `固定且可预测的步骤适合 Pipeline；需要按状态选择下一位专家时适合 Supervisor 或带条件边的 Graph；大量专家围绕持续变化的共享证据工作时适合 Blackboard。选择依据是依赖关系和状态流，而不是 Agent 数量。`,
    `Supervisor 的瓶颈是所有进度和结果集中回传主管，容易形成上下文、吞吐和单点决策瓶颈；Blackboard 的瓶颈是共享空间中的并发写入、重复信息、过期事实和权限视图管理。`,
    `任务依赖层画“谁必须等待谁的产物”，信息依赖层画“谁能读写哪些消息、证据或状态”，权力依赖层画“谁能分配任务、批准工具、驳回结果和终止运行”。三层叠加后才能看出真正的协作拓扑。`
  ],
  "06-通信协议.md": [
    `路由字段至少包括 sender、receiver、conversation_id、task_id；语义字段包括 message_type、intent、payload_schema；上下文字段包括 parent_id、artifact_refs、evidence_refs；控制字段包括 message_id、trace_id、idempotency_key、TTL、priority 和 retry_policy。`,
    `执行端先以 idempotency_key 查询持久化记录；若同一动作已经成功，就直接返回先前结果或产物引用，不再调用工具。该检查必须和结果写入形成原子边界，否则并发重试仍可能执行两次。`,
    `先用 question 事件把 Task 置于 <code>INPUT_REQUIRED</code>，再发送 final 事件；测试应断言验证器返回非法事件错误、Task 仍为 INPUT_REQUIRED、完成回调没有执行，并产生包含原状态、事件和拒绝原因的审计记录。`,
    `Publisher confirm 只表示消息代理已接管发布；consumer ACK 表示某次投递已处理到约定的持久边界；Task COMPLETED 表示服务端任务进入成功终态并绑定 Artifact；客户端还要查询 Task、读取并校验 Artifact 才能完成业务验收。四者不能互相替代。`
  ],
  "07-路由与交接.md": [
    `安全和权限规则、显式消息类型、终止状态、预算上限、资源所有权及确定性业务规则应先于模型判断。模型适合处理语义模糊的专家选择，不能覆盖硬约束。`,
    `一个自足交接包应包含 task_id、目标、已完成步骤、当前状态、关键决定、证据与产物引用、未解决问题、验收条件、权限范围和下一动作。接收者据此工作，不需要重读完整聊天记录。`,
    `权限失败应拒绝执行并转交审批者；普通可恢复失败应在受限次数内重试或改用替代 Worker；主管失败应由外层 watchdog、备用主管或人工接管，并从持久化检查点恢复，不能让主管自己批准自己的越权重试。`
  ],
  "08-共享记忆与上下文.md": [
    `工作记忆保存当前任务最近步骤；共享记忆保存团队需要共同读取的事实和产物；情节记忆记录一次任务发生了什么及结果；语义记忆沉淀跨任务仍稳定的知识。它们的生命周期、读者和写入门槛不同。`,
    `值得长期保存的是“某 API 在版本 3 已弃用，并附官方来源和验证日期”；应丢弃的是“模型猜测用户可能喜欢某方案”或无来源的临时推断。长期记忆应满足可复用、可验证、可更新和权限允许。`,
    `Developer 上下文应包含需求、接口契约、相关代码、可写范围和测试失败；Reviewer 应包含验收标准、候选补丁、测试证据、风险清单和只读引用。两者共享任务事实，但不必共享全部思考记录或相同工具权限。`
  ],
  "09-辩论评审与共识.md": [
    `候选生成阶段独立产生答案；评审阶段按 Rubric、证据或测试指出优缺点；聚合阶段选择、排序或合并候选；最终路由阶段决定发布、返工、升级人工还是继续收集证据。把四步混在一次提示里会丢失独立性和审计点。`,
    `多数票会放大相关错误；裁判模型会受措辞、长度、自信程度和同源模型偏好影响；测试选择会被不完整测试、错误 oracle 或只优化可测指标误导。因此三者都需要对照集和独立评测。`,
    `最终结果应同时保存 chosen_answer、supporting_evidence、dissenting_views、unresolved_risks 和 decision_reason。少数意见不能只留在聊天记录里，因为它可能指出尚未覆盖的边界条件或共同假设。`
  ],
  "10-观测评测与调试.md": [
    `按同一 trace_id 查询事件，再按 timestamp 与 parent_span_id 排序：先看到 plan 事件，随后是 tool/action 执行及 observation，再是 test，最后是 review 和 final。每一步还应关联 agent_id、task_id、artifact_id 和输入输出哈希。`,
    `任务成功率衡量最终任务达到验收标准的比例；路由准确率衡量分配给正确角色或工具的比例；每次成功成本等于总模型、工具和人工成本除以成功任务数。它们分别回答结果、过程决策和经济效率。`,
    `可重放记录还需要模型提供方与精确版本、采样参数、系统提示版本、上下文快照或哈希、工具版本、参数、外部数据版本、随机种子、权限策略版本和环境镜像。只有 trace 文本通常无法复现。`
  ],
  "11-安全与生产化.md": [
    `Developer 读取 <code>secret://production/api-key</code> 应返回 <code>resource_out_of_scope</code>；Reviewer 请求 <code>file.write</code> 应返回 <code>action_not_allowed</code>；Operator 获批后修改版本参数，应返回 <code>approval_missing_expired_or_mismatched</code>。授权必须在工具入口按真实请求重新计算，不能只根据角色名称放行。`,
    `不可信数据标记只能帮助系统保留来源并提醒模型区分证据与指令，无法阻止模型被绕过、工具被其他调用方请求或参数指向越权资源。执行端授权根据真实身份、动作、资源、参数和审批记录做确定性拒绝，二者处于不同安全层。`,
    `文件修改通常可用版本化快照、前后哈希和原子替换回滚；外部消息提交后无法撤回，只能延迟发送或发送更正等补偿；永久删除和数据泄露不可逆，必须以前置审批、软删除和最小影响范围预防。自动回滚前要确认资源仍是本次变更版本，恢复后还要验证业务健康；冲突或恢复失败时升级人工。`
  ],
  "12-案例软件工厂.md": [
    `ChatDev 的公开锚点是 <code>chatdev1.0</code> 分支中的 <code>CompanyConfig/Default/ChatChainConfig.json</code>、<code>PhaseConfig.json</code> 和 <code>RoleConfig.json</code>；MetaGPT 的锚点是 <code>metagpt/software_company.py</code>、<code>metagpt/team.py</code>、<code>metagpt/roles/*.py</code> 以及其中的 Team、Role、Action 对象。`,
    `ChatDev 用 JSON 配置声明阶段链、组合阶段和角色提示，运行器解释配置；MetaGPT 用 Python 对象实例化 Team，hire Role，并由 Action 与消息订阅表达 SOP。前者主要修改数据配置，后者主要组合和扩展对象行为。`,
    `标明仓库路径、分支和对象名的短片段属于原始代码摘录；围绕它们绘制的流程图、概念卡、删减后的伪执行顺序和中文说明属于教学解释。教学抽象可以帮助理解，但不能声称与源码逐行等价。`
  ],
  "13-案例研究团队.md": [
    `<code>Arxiv Search Agent</code> 只使用 arXiv 搜索工具找论文，<code>Google Search Agent</code> 使用网页搜索工具找公开网页，<code>Report Agent</code> 汇总已有搜索结果并写综述，不应自行伪造检索证据。`,
    `<code>FunctionTool</code> 是最底层的函数与参数 Schema 包装；<code>AssistantAgent</code> 把模型、系统提示和一个或多个工具组成角色；<code>RoundRobinGroupChat</code> 在团队层决定这些 Agent 的发言顺序和终止检查。`,
    `还需要最大轮数、token/费用预算、工具超时与重试上限、Schema 校验、搜索来源白名单或审计、重复查询检测、人工取消以及最终报告完整性检查。终止词只是一个信号，不是完整运行护栏。`
  ],
  "14-框架与项目图谱.md": [
    `先用官方文档确认对外承诺，再用官方仓库确认版本和维护状态，最后定位实现该概念的源码对象、模块或测试。例如对 Team 的说法应同时给出概念页、仓库链接和具体类路径。三类证据相互补足。`,
    `对话型框架以消息轮次为中心；SOP 型用角色与动作编码标准流程；任务型把 Task、依赖和产物作为一等对象；handoff 型通过返回另一 Agent 转移控制权；状态图型用节点、边和持久状态表达分支、循环与恢复。`,
    `带精确仓库路径和保持原语义的片段可标为原始实现；为解释概念而删减、改名、合并步骤或自行编写的代码必须标为教学抽象。无法核验来源时，只能说“示意”或“建议设计”。`
  ],
  "15-学习路线与引用.md": [
    `设计路线按定义、理论、角色、任务、拓扑、协议、路由和共享状态递进；工程路线优先协议、交接、共享状态、质量、观测和安全；研究路线以理论、拓扑、共识、评测和源码核验为主。顺序分别服务于组织设计、运行可靠性和因果验证。`,
    `例如为 Supervisor 路由练习保留规则路由基线；指标至少包含路由准确率、任务成功率、每次成功成本和人工介入率；再注入未知任务、越权请求、Worker 超时和主管故障，检查拒绝、升级与恢复路径。`,
    `同预算比较排除“只是用了更多 token”的解释；消融定位真正有贡献的角色或通信边；失败分类说明平均分背后是路由、协议、工具、共识还是治理问题。三者共同回答复杂团队为何有效、在哪里失效。`,
    `先从论文确认研究问题和实验设置，再从官方文档定位框架概念，最后在指定版本仓库中核对 Team、Role、Message、Task 或 Graph 对象。教学伪代码和二手示例只能帮助理解，不能替代源码证据。`
  ],
  "market-task-auction.md": [
    `<code>quality - 0.2 * cost</code> 只是按自报数字排序，没有付款规则、真实性约束或虚报惩罚。只要夸大 quality 或压低 cost 能提高中标概率，理性的竞标者就有操纵动机，因此它不能保证诚实报价。`,
    `代码任务可用隐藏测试通过率、回归数、静态分析和审查缺陷；研究任务可用引用可核验率、覆盖率、事实错误率和新证据率；安全任务可用漏洞检出率、误报率、严重度加权召回和越权阻断率。质量定义必须随任务类型变化。`,
    `“擅长 Python”不能直接外推到安全审计，旧成绩也可能因模型或工具更新失效；少量样本的高分不稳定。因此信誉记录必须带 task_scope、时间衰减、样本量和置信区间，并保留探索新 Agent 的份额。`,
    `SALE 主要让 Agent 以解决计划竞标并由共享拍卖记忆改进计划估值；DRF 主要用历史交互估计能力与诚实度并进行动态过滤；STAR 进一步讨论图工作流中的反向拍卖、上下文转移成本和激励相容。三者解决的是不同层级。`,
    `封闭企业团队通常没有真实支付和独立经济利益，主要风险是错配、虚高自评和履约失败。先做能力硬过滤、任务后验证和历史校准，就能解决大部分工程问题；过早引入复杂支付机制会增加解释、合规和维护成本。`
  ],
  "topology-blackboard.md": [
    `群聊记录是按时间追加的对话，消息语义常依赖前文；黑板是按类型组织、可查询和可更新的共享状态，卡片具有键、版本、来源和生命周期。Agent 通过读写状态间接协作，而不是必须逐条阅读所有发言。`,
    `来源回答“谁基于什么写下它”，版本支持冲突检测和更新，有效期阻止旧事实继续驱动决策。缺少任一项，黑板都可能把传言、覆盖写入或过期数据伪装成当前共识。`,
    `角色视图只投影该角色任务所需且有权读取的卡片字段，既减少进入提示的 token，也避免暴露敏感内容。写权限也应按卡片类型和字段限制，并把拒绝事件写入审计日志。`,
    `当专家数量较多、任务由新证据机会式触发、多个角色需要复用同一事实且执行时间较长时选择 Blackboard；当流程需要一个中心持续分解、分派和重规划时 Supervisor 更直接。`
  ],
  "topology-debate.md": [
    `同质 Agent 往往共享模型偏差和初始错误，后续轮次还会相互锚定；多说几轮增加成本，却不一定增加独立证据。简单多数票至少保留初始独立候选，而普通辩论可能把多数意见扩散成一致错误。`,
    `多样性增加不同模型、证据和推理路径，使系统有机会产生真正不同的候选；校准置信度让聚合器知道某个 Agent 在类似任务上多可靠，避免错误但过度自信的发言主导讨论。`,
    `Judge 也会偏好更长、更流畅、更自信或与自己同源的答案。必须用带人类标注、可执行测试和对抗样例的数据单独评测其准确率、偏差与校准度，而不能把 Judge 输出直接当作真值。`,
    `答案已实质一致、置信度差异很小、连续一轮没有新增证据或反例、达到轮数/预算上限，都说明边际信息接近零。仍有高风险分歧时应转外部验证或人工，而不是无限续轮。`
  ],
  "topology-dynamic.md": [
    `团队选择属于“从注册表挑哪些 Agent”；边生成属于“决定选中 Agent 之间怎样通信”；自动系统设计还会生成角色、提示、工具组合、控制逻辑甚至代码。后者的搜索空间和验证责任最大。`,
    `G-Designer 面向任务设计通信图；AgentPrune 从已有时空通信图中剪除冗余或恶意边；ARG-Designer 自回归决定 Agent 数量、角色和边；GoAgent 以协作小组为生成单元，同时考虑组内凝聚与组间协调。`,
    `拓扑提案来自概率模型，可能违反权限、数据驻留、职责分离或预算硬约束。它必须先通过策略验证、静态检查和沙箱评测，工具凭据仍由独立执行端按最小权限发放，不能随提案自动继承。`,
    `固定总 token、模型调用数、工具预算和时间上限，在同一任务集与随机种子上比较固定基线和动态方案；同时报告成功率、成本、重配置次数、拓扑生成开销、方差、安全违规和失败恢复率。`
  ],
  "topology-graph.md": [
    `Pipeline、Supervisor 和 Debate 可以分别表示为路径图、中心星型图和分层或循环图，但普通拓扑图通常只描述连接形状。可执行 Graph 还要定义节点运算、结构化状态、边条件、并行合并、循环终止、检查点和错误策略。`,
    `Graph 可以把 Supervisor 作为语义路由节点，把多个 Researcher 作为并行分支，把证据引用写入 Blackboard，再把 Debate 封装成质量子图；父图负责这些组件之间的 fan-out、fan-in、返工边、人工门禁和结束条件。`,
    `并行节点可能同时更新同一字段，reducer 决定覆盖、追加、去重或自定义冲突处理；join 条件决定 all-of、any-of、quorum 或截止时间到达后何时启动下游。没有二者，图只能画出并行箭头，不能稳定执行。`,
    `沿预先存在的回边循环只改变当前路径，节点和边集合仍然不变；动态拓扑会在运行时真正创建、删除或重连节点与边，因此需要额外保存图版本、生成依据、权限审批和回退结构。`,
    `业务状态保存计划、产物、测试和预算；控制状态保存刚完成节点、下一 super-step、子图命名空间和图版本。复杂 Graph 可能有多个并行分支部分完成，只保存业务数据无法判断恢复后应调度哪些节点。`,
    `GPTSwarm 区分节点 Prompt 优化与边连接优化；AFlow 搜索代码表示的工作流和算子组合；AgentPrune 剪除低价值或有害通信边；AAFLOW 主要优化已确定工作流的底层调度与数据流。`
  ],
  "topology-pipeline.md": [
    `顺序调用只描述执行次序，没有定义阶段输入输出契约、验收门禁、检查点、重试边界、幂等、背压和可观测性。任一 Agent 返回错误或超长结果时，系统不知道该拒绝、恢复还是继续。`,
    `AFlow 主要用搜索方法优化 Agent 工作流的设计与算子组合；AAFLOW 主要关注已确定工作流的分布式运行，包括数据传递、调度、异步批处理和复现性。一个偏“生成什么流程”，一个偏“怎样高效执行流程”。`,
    `阶段门禁阻止不合格产物进入下游；检查点保存最近一次通过门禁的状态和产物版本。失败后运行器回到该检查点，只重做未通过的阶段，而不必从任务起点重新执行全部 Agent。`,
    `出现条件分支、循环、并行汇合或复杂恢复路径时升级到 Graph；需要根据语义和进度动态选择专家、重规划或管理开放任务时升级到 Supervisor。固定、稳定、可预测的阶段仍应保留 Pipeline。`
  ],
  "topology-supervisor.md": [
    `任务账本保存目标、子任务、依赖、负责人、约束和验收标准；进度账本保存已经完成什么、当前阻塞、证据、产物、预算消耗和下一步。前者描述计划结构，后者描述运行事实。`,
    `能力和权限是不可妥协的硬约束：不会使用所需工具或无权读取资源的 Worker，即使语言评分很高也不能被选择。先过滤再排序还能缩小模型路由的候选集，降低幻觉式分配。`,
    `Magentic-One 的 Orchestrator 持有任务与进度账本，持续规划、分派、检查结果并在停滞时重规划；普通路由函数通常只根据当前输入选择一次目的地，不负责长期状态、完成验证和失败恢复。`,
    `多专家围绕共享证据机会式响应时用 Blackboard；步骤稳定且可预先验收时降为 Pipeline；控制流包含明确分支、循环、回退和持久恢复时用 Graph。主管不应成为所有问题的默认中心。`
  ]
};
