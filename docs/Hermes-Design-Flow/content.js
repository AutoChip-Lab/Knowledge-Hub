// 自动生成，请勿手改。修改 Markdown 后运行 python build.py。
window.SITE = {"title": "Hermes Agent 技术导览", "titleEn": "Hermes Agent Technical Guide", "subtitle": "沿着 run_conversation，讲清核心循环、自改进、子代理和持久会话。", "subtitleEn": "Follow run_conversation through the core loop, self-improvement, subagents, and durable sessions.", "source": {"commit": "540f90190f50f9518bf36632a724e0e58877a10b", "shortCommit": "540f90190f", "commitDate": "2026-07-09T19:09:38-07:00", "repository": "https://github.com/NousResearch/hermes-agent", "commitUrl": "https://github.com/NousResearch/hermes-agent/commit/540f90190f50f9518bf36632a724e0e58877a10b", "dirty": false}, "phaseOrder": ["核心设计", "运行机制", "状态与上下文"], "stages": [{"num": "①", "prefix": "01", "title": "Hermes 核心架构", "titleEn": "Hermes Core Architecture", "phase": "核心设计", "phaseEn": "Core Design", "summary": "多个入口怎样共享一个 AIAgent，以及核心循环和边缘能力怎样分工。", "summaryEn": "How multiple entry points share one AIAgent, and how the core loop divides work with edge capabilities.", "codeAnchors": [{"title": "AIAgent 对外入口", "path": "run_agent.py", "start": 5775, "end": 5825, "note": "AIAgent 接收用户输入，并把正常 turn 转给统一 conversation loop。", "range": "run_agent.py:L5775-L5825", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/run_agent.py#L5775-L5825"}, {"title": "统一 Conversation Loop", "path": "agent/conversation_loop.py", "start": 523, "end": 646, "note": "准备 TurnContext 后，进入共享的模型—工具循环。", "range": "agent/conversation_loop.py:L523-L646", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/agent/conversation_loop.py#L523-L646"}, {"title": "工具统一执行入口", "path": "agent/tool_executor.py", "start": 306, "end": 390, "note": "模型产生的 tool calls 最终由同一个执行层运行并回填。", "range": "agent/tool_executor.py:L306-L390", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/agent/tool_executor.py#L306-L390"}, {"title": "SessionDB", "path": "hermes_state.py", "start": 871, "end": 930, "note": "会话持久化独立于入口和模型运行时。", "range": "hermes_state.py:L871-L930", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/hermes_state.py#L871-L930"}], "raw": "# Hermes 的核心到底是什么\n\n先说结论：Hermes 的核心就是一个共享的 `AIAgent`，里面最重要的是 `run_conversation()` 模型—工具循环。\n\nCLI、TUI、消息平台和 IDE 看起来是不同产品，但它们主要负责接收输入和展示结果。真正决定“这轮对话怎么跑”的，仍然是同一个 Agent core。\n\n## 1. 一条最简单的调用链\n\n```mermaid\nflowchart TD\n    A[\"CLI / TUI / Gateway / IDE<br/>接收用户输入，找到对应 session\"]\n    B[\"AIAgent.run_conversation()<br/>把一次用户输入交给统一核心<br/>run_agent.py:L5775\"]\n    C[\"conversation_loop.run_conversation()<br/>维护 messages，反复调用模型和工具<br/>conversation_loop.py:L523\"]\n    D[\"Provider adapter<br/>把统一请求转换成具体模型协议\"]\n    E[\"ToolExecutor<br/>执行工具，并把结果写回 messages\"]\n    F[\"SessionDB<br/>保存会话，供下一轮或重启后恢复\"]\n\n    A --> B --> C\n    C <--> D\n    C <--> E\n    C --> F\n```\n\n不管输入来自哪里，进入 `run_conversation()` 之后，核心数据流基本相同：\n\n```text\n用户输入 + 旧会话\n  → messages\n  → 调用模型\n  → 模型返回文本或 tool_calls\n  → 执行工具并把结果追加到 messages\n  → 再次调用模型\n  → 得到最终回复并保存会话\n```\n\n第 2 章会沿着源码详细展开这条 loop。\n\n## 2. 五部分各自负责什么\n\n| 部分 | 主要职责 | 不负责什么 |\n|---|---|---|\n| 入口 | 接收消息、鉴权、选择 session、展示流式结果 | 不重新实现 Agent loop |\n| `AIAgent` | 保存配置和运行状态，提供 `run_conversation()` | 不直接实现所有外部能力 |\n| Conversation loop | 组装 context、调用模型、执行工具、控制预算和压缩 | 不关心 Telegram 或 TUI 怎样显示 |\n| Tools / MCP / Plugins | 读文件、跑命令、浏览网页、连接外部系统 | 不拥有主会话流程 |\n| SessionDB / Memory / Skills | 保存会话、长期事实和可复用流程 | 不代替当前 `messages` 做实时推理 |\n\n整体结构就是：中间是一条稳定的 Agent 运行链，上面接不同入口，下面接不同模型、工具和状态系统。\n\n## 3. Core 里面真正统一的东西\n\n### 统一 `messages`\n\n模型看到的 user、assistant、tool call 和 tool result，最终都围绕同一份 `messages` 变化。不同入口不维护各自的对话格式。\n\n### 统一模型—工具循环\n\n模型要么返回最终文本，要么请求工具。工具结果写回 `messages` 后，继续调用模型。CLI 和 Gateway 不需要各写一套循环。\n\n### 统一运行控制\n\n以下行为也放在同一条 core loop 中：\n\n- 最大迭代次数和共享预算；\n- 用户中断；\n- context compression；\n- provider fallback；\n- turn 结束后的持久化和 background review。\n\n## 4. 为什么说 Core 比较轻\n\nCore 主要做编排，不负责实现所有能力：\n\n```text\nCore 决定：\n  现在该把哪些 messages 发给模型？\n  模型要调用哪个工具？\n  工具结果怎样回到下一轮？\n  什么时候结束、压缩或持久化？\n\n边缘模块负责：\n  Telegram 消息怎样收发？\n  Anthropic/OpenAI 请求怎样编码？\n  浏览器、终端、MCP 工具怎样执行？\n  Memory 和 Skills 怎样落盘？\n```\n\n因此可以把 Hermes 理解成：一个不大的 Agent loop，加上一组围绕真实模型和真实运行环境积累的规则与扩展。复杂度很高，但多数复杂度位于 provider、工具、入口和状态边界，不需要全部塞进 core abstraction。\n\n## 5. 为什么新能力通常放在边缘\n\n每增加一个 core tool，它的 schema 都可能跟随每次模型请求，占用 context 并影响 prompt cache。Hermes 因此优先选择：\n\n```text\n扩展现有工具\n  → CLI command + Skill\n  → 按配置启用的工具\n  → Plugin / MCP\n  → 最后才考虑新增 core tool\n```\n\n这样可以让入口、模型和能力不断增加，同时保持 `run_conversation()` 这条主链相对稳定。\n\n## 6. 后面四章分别讲什么\n\n- **第 2 章，执行闭环：** `messages` 怎样一步步变成模型请求，工具结果怎样写回来。\n- **第 3 章，Memory 与自改进：** 前台和后台 Agent 怎样通过工具更新长期知识。\n- **第 4 章，工具与子代理：** 什么时候直接调用工具，什么时候运行代码或创建 Child Agent。\n- **第 5 章，会话存储：** `messages` 怎样增量写入 SessionDB，压缩后的历史怎样恢复和搜索。\n\n一句话记住第一章：\n\n> 多个入口共享一个 `AIAgent`；`run_conversation()` 负责循环，模型、工具和状态能力都挂在这条循环的边缘。\n", "available": true, "file": "01-Hermes核心架构.md", "rawEn": "# What Is the Hermes Core?\n\nThe short answer: Hermes has one shared `AIAgent`. Its most important mechanism is the model–tool loop in `run_conversation()`.\n\nCLI, TUI, messaging platforms, and IDE integrations look like different products, but they mainly receive input and present results. The same agent core decides how each turn runs.\n\n## 1. The simplest call chain\n\n```mermaid\nflowchart TD\n    A[\"CLI / TUI / Gateway / IDE<br/>Receive input and select a session\"]\n    B[\"AIAgent.run_conversation()<br/>Send one user turn into the shared core<br/>run_agent.py:L5775\"]\n    C[\"conversation_loop.run_conversation()<br/>Maintain messages and repeat model/tool calls<br/>conversation_loop.py:L523\"]\n    D[\"Provider adapter<br/>Translate the common request into a model protocol\"]\n    E[\"ToolExecutor<br/>Run tools and append their results to messages\"]\n    F[\"SessionDB<br/>Persist the conversation for later turns and resume\"]\n\n    A --> B --> C\n    C <--> D\n    C <--> E\n    C --> F\n```\n\nRegardless of the entry point, the central data flow is the same:\n\n```text\nuser input + previous conversation\n  → messages\n  → call the model\n  → receive text or tool_calls\n  → execute tools and append results to messages\n  → call the model again\n  → return the final answer and persist the session\n```\n\nChapter 2 follows this loop line by line.\n\n## 2. Responsibilities of the five parts\n\n| Part | Owns | Does not own |\n|---|---|---|\n| Entry point | Input, authorization, session selection, streaming UI | A separate agent loop |\n| `AIAgent` | Configuration, runtime state, and `run_conversation()` | Every external capability |\n| Conversation loop | Context, model calls, tools, budgets, compression | Telegram or TUI rendering |\n| Tools / MCP / Plugins | Files, commands, browser, external systems | The primary conversation |\n| SessionDB / Memory / Skills | Sessions, durable facts, reusable procedures | Live reasoning over `messages` |\n\nThe architecture has one stable agent path in the middle, with many entry points above it and many models, tools, and state systems below it.\n\n## 3. What the core standardizes\n\n### One `messages` representation\n\nUser messages, assistant replies, tool calls, and tool results all evolve around the same `messages` list. Entry points do not maintain separate conversation formats.\n\n### One model–tool loop\n\nThe model either returns final text or requests tools. Tool results are appended to `messages`, then the model runs again. CLI and Gateway do not implement separate loops.\n\n### One runtime control path\n\nThe same core loop owns iteration budgets, interrupts, context compression, provider fallback, persistence, and post-turn background review.\n\n## 4. Why the core stays relatively small\n\nThe core orchestrates; edge modules implement capabilities.\n\n```text\nCore decides:\n  Which messages should the model see now?\n  Which tool did the model request?\n  How does the result enter the next iteration?\n  When should the turn stop, compress, or persist?\n\nEdge modules decide:\n  How does Telegram send a message?\n  How is an Anthropic or OpenAI request encoded?\n  How do browser, terminal, and MCP tools execute?\n  How are Memory and Skills written to disk?\n```\n\nHermes is therefore a compact agent loop surrounded by practical rules and extensions for real providers and real environments.\n\n## 5. Why new capabilities usually live at the edge\n\nEvery core tool schema can be sent on every model call, consuming context and affecting prompt caching. Hermes therefore prefers this order:\n\n```text\nextend an existing tool\n  → CLI command + Skill\n  → configuration-gated tool\n  → Plugin / MCP\n  → new core tool only as a last resort\n```\n\n## 6. What the remaining chapters cover\n\n- **Chapter 2 — Execution Loop:** how `messages` becomes a request and receives tool results.\n- **Chapter 3 — Memory and Self-Improvement:** how foreground and review agents update durable knowledge.\n- **Chapter 4 — Tools and Subagents:** when to call a tool, execute code, or create a child agent.\n- **Chapter 5 — Session Storage:** how messages reach SessionDB and remain searchable after compression.\n\n> Multiple entry points share one `AIAgent`; `run_conversation()` owns the loop, while model, tool, and state capabilities remain at its edges.\n", "fileEn": "en/01-core-architecture.md"}, {"num": "②", "prefix": "02", "title": "执行闭环", "titleEn": "Execution Loop", "phase": "运行机制", "phaseEn": "Runtime", "summary": "从 run_conversation 追踪 messages、模型请求、压缩、工具回填和收尾。", "summaryEn": "Trace messages, model requests, compression, tool results, and finalization from run_conversation.", "codeAnchors": [{"title": "TurnContext：建立本轮 messages", "path": "agent/turn_context.py", "start": 271, "end": 347, "note": "复制历史、追加 user、取得 cached system prompt，并提前持久化 inbound turn。", "range": "agent/turn_context.py:L271-L347", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/agent/turn_context.py#L271-L347"}, {"title": "messages 投影为 api_messages", "path": "agent/conversation_loop.py", "start": 787, "end": 882, "note": "动态 context 只进入请求副本；system prompt 在请求时前置。", "range": "agent/conversation_loop.py:L787-L882", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/agent/conversation_loop.py#L787-L882"}, {"title": "请求前压力检查", "path": "agent/conversation_loop.py", "start": 954, "end": 1058, "note": "按 messages、system 和 tools 的完整规模判断压缩，压缩后重建请求。", "range": "agent/conversation_loop.py:L954-L1058", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/agent/conversation_loop.py#L954-L1058"}, {"title": "工具回填闭环", "path": "agent/conversation_loop.py", "start": 4660, "end": 4784, "note": "assistant tool_calls 和 tool results 写回同一个 messages，再进入下一轮。", "range": "agent/conversation_loop.py:L4660-L4784", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/agent/conversation_loop.py#L4660-L4784"}, {"title": "压缩五阶段", "path": "agent/context_compressor.py", "start": 2793, "end": 3055, "note": "裁剪 tool results，保护 head/tail，总结 middle，再重组 transcript。", "range": "agent/context_compressor.py:L2793-L3055", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/agent/context_compressor.py#L2793-L3055"}, {"title": "统一收尾", "path": "agent/turn_finalizer.py", "start": 30, "end": 120, "note": "循环退出后统一完成持久化、结果组装和后台 review。", "range": "agent/turn_finalizer.py:L30-L120", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/agent/turn_finalizer.py#L30-L120"}], "raw": "# `run_conversation()`：一次 turn 怎样完成\n\nHermes core 的主干很短：准备本轮 context，循环调用模型和工具，最后收尾。教学时始终区分三份数据：\n\n| 数据 | 含义 | 是否持久化 |\n|---|---|---|\n| `messages` | 当前会话的 canonical transcript，loop 的核心状态 | 是 |\n| `active_system_prompt` | 当前 session 冻结的 system prompt | 随 session 保存 |\n| `api_messages` | 某一次模型请求从前两者投影出的临时副本 | 否 |\n\n## 1. 总流程\n\n```mermaid\nflowchart TD\n    A[\"run_conversation()<br/>一次完整的用户prompt turn<br/>conversation_loop.py:L523\"]\n    B[\"build_turn_context()<br/>复制历史、追加 user prompt，并取得缓存 system prompt<br/>turn_context.py:L119\"]\n    C[\"messages 和 active_system_prompt<br/>返回主循环需要的两份核心输入\"]\n    D[\"Agent loop 开始<br/>反复执行模型请求，追加工具结果\"]\n    E[\"请求构造<br/>从 messages 构造本次 api_messages<br/>L787–954\"]\n    F{\"messages 是否需要压缩？<br/>L954–1058\"}\n    G[\"agent._compress_context()<br/>压缩并替换 messages，然后重建请求\"]\n    H[\"模型 API 调用<br/>根据 api_messages 生成 assistant response\"]\n    I{\"有 tool_calls？\"}\n    J[\"_execute_tool_calls()<br/>执行工具，并把 assistant/tool results 追加到 messages<br/>L4660–4784\"]\n    K[\"保存最终 assistant 输出文本<br/>没有工具，关闭当前 Agent loop<br/>L4786–5234\"]\n    L[\"finalize_turn()<br/>持久化 messages、组装结果并触发后台 review 工作（见第3章）<br/>turn_finalizer.py:L30\"]\n\n    A --> B --> C --> D --> E --> F\n    F -- 是 --> G --> E\n    F -- 否 --> H --> I\n    I -- 是 --> J --> D\n    I -- 否 --> K --> L\n```\n\n真正推动循环的只有 `messages`：模型输出和工具结果不断追加进去，下一轮再据此重建请求。\n\n## 2. `build_turn_context()` 做什么\n\n入口在 `agent/conversation_loop.py:L592–622`，实现位于 `agent/turn_context.py:L119–562`。与 context 直接相关的工作是：\n\n```text\nconversation_history\n  → 浅复制成 messages                         L271–272\n  → 恢复 todo 和周期计数                      L274–314\n  → 追加本轮 user message                     L316–320\n  → 恢复或首次构建 _cached_system_prompt       L329–333\n  → 提前持久化 inbound user                    L335–347\n  → 必要时执行 turn 开始前的 preflight 压缩    L350–460\n  → 取得 plugin context / external recall\n  → 返回 TurnContext                          L553–562\n```\n\n所以它不是直接返回“最终 API 请求”，而是返回 loop 的工作状态：`messages`、`active_system_prompt`、当前 user 的位置，以及只供本轮请求使用的动态 context。\n\n## 3. `messages` 怎样变成模型请求\n\n每次 loop 都重新投影，核心代码在 `agent/conversation_loop.py:L787–954`：\n\n```text\nmessages\n  → 逐条 copy 为 api_messages\n  → 仅在当前 user 副本追加 external memory / plugin context\n  → 前置 active_system_prompt + ephemeral_system_prompt\n  → 插入可选 prefill\n  → 与 tools 一起形成最终请求\n```\n\n关键边界是：动态 recall 和 plugin context 只进入 `api_messages`，不改用户原话，也不写进 SessionDB；system prompt 通常也不在 `messages` 中，而是在每次请求前临时加到最前面。\n\n## 4. 三个压缩检查点\n\n为了避免一轮内新增的大型 tool result 撑爆 context，压缩不是只检查一次：\n\n1. **Turn 开始前**：`turn_context.py:L350–460`，检查“历史 + 新 user + system + tools”。\n2. **API 请求前**：`conversation_loop.py:L954–1058`，检查刚拼好的完整请求；压缩后丢弃这份 `api_messages`，从新 `messages` 重建。\n3. **工具执行后**：`conversation_loop.py:L4733–4778`，结合 provider 报告的真实 prompt tokens 再检查。\n\nAPI 的 context-overflow 恢复也可能触发压缩，但属于异常恢复，不是理解主循环所必需的分支。\n\n### 压缩调用链\n\n```text\nAIAgent._compress_context()                    run_agent.py:L5608\n  → conversation_compression.compress_context()  conversation_compression.py:L435\n  → agent.context_compressor.compress()          conversation_compression.py:L638\n  → ContextCompressor.compress()                  context_compressor.py:L2793\n```\n\n传入底层算法的核心参数是：\n\n```python\ncompress(\n    messages,\n    current_tokens=approx_tokens,\n    focus_topic=focus_topic,\n    force=force,\n)\n```\n\n### 五阶段算法\n\n```mermaid\nflowchart LR\n    A[\"_prune_old_tool_results()<br/>删除较早的大型工具结果<br/>L2852–2858\"] -->\n    B[\"_protect_head_size()<br/>保留开头的会话锚点<br/>L2860–2863\"] -->\n    C[\"_find_tail_cut_by_tokens()<br/>按 token budget 保留近期消息<br/>L2864–2885\"] -->\n    D[\"_generate_summary()<br/>用旧 summary、middle 和 prompt 调用 LLM<br/>L2886–2935\"] -->\n    E[\"重组 transcript<br/>拼成 head + summary + tail<br/>L2997 起\"]\n```\n\n已有 handoff summary 时，它会和本次新增的中间段一起进入同一次总结请求，形成迭代更新。压缩结果会真正替换 `messages`，随后同步 SessionDB，并重建本次请求；它不是只缩短临时的 `api_messages`。\n\n## 5. 模型结果怎样回到 `messages`\n\n模型返回 tool calls 时：\n\n```text\nassistant(tool_calls) → messages               conversation_loop.py:L4660\n先增量持久化 tool-call block                   L4662–4667\nagent._execute_tool_calls(...)                  L4688\ntool results → 同一个 messages                  tool_executor.py:L918–962\n必要时压缩                                     L4733–4778\ncontinue agent loop                             L4780–4784\n```\n\n模型没有调用工具时，最终 assistant message 写入 `messages`，然后 `break`：`agent/conversation_loop.py:L4786–5234`。\n\n## 6. `finalize_turn()`\n\n所有 loop 出口汇入 `agent/turn_finalizer.py:L30`。核心职责是：\n\n- 清理临时恢复消息，保证 transcript 以合法 assistant turn 结束；\n- 把最终 `messages` 持久化到 SessionDB；\n- 生成包含 `final_response`、`messages`、usage 和 session 信息的返回值；\n- 同步 external memory，并在满足条件时启动第 3 章的后台 review。\n\n最终的数据闭环是：\n\n```text\n本轮 result[\"messages\"]\n  → SessionDB / 调用方保存\n  → 下一轮 conversation_history\n  → build_turn_context()\n  → 新的 messages\n```\n", "available": true, "file": "02-执行闭环.md", "rawEn": "# `run_conversation()`: How One Turn Runs\n\nThe Hermes core prepares context, loops over model and tool calls, then finalizes the turn. Keep three data structures separate:\n\n| Data | Meaning | Persisted? |\n|---|---|---|\n| `messages` | Canonical transcript and main loop state | Yes |\n| `active_system_prompt` | System prompt frozen for this session | With the session |\n| `api_messages` | Temporary projection for one model request | No |\n\n## 1. Main flow\n\n```mermaid\nflowchart TD\n    A[\"run_conversation()<br/>Run one complete user turn<br/>conversation_loop.py:L523\"]\n    B[\"build_turn_context()<br/>Copy history, append user, restore cached system prompt<br/>turn_context.py:L119\"]\n    C[\"messages + active_system_prompt<br/>The two core loop inputs\"]\n    D[\"Agent loop<br/>Repeat model requests and tool-result appends\"]\n    E[\"Request projection<br/>Build api_messages from messages<br/>L787–954\"]\n    F{\"Compress?<br/>L954–1058\"}\n    G[\"agent._compress_context()<br/>Replace messages and rebuild the request\"]\n    H[\"Model API call<br/>Generate an assistant response from api_messages\"]\n    I{\"tool_calls?\"}\n    J[\"_execute_tool_calls()<br/>Execute tools and append assistant/tool results<br/>L4660–4784\"]\n    K[\"Save final assistant text<br/>No tools: leave the Agent loop<br/>L4786–5234\"]\n    L[\"finalize_turn()<br/>Persist messages, build the result, start background work<br/>turn_finalizer.py:L30\"]\n\n    A --> B --> C --> D --> E --> F\n    F -- Yes --> G --> E\n    F -- No --> H --> I\n    I -- Yes --> J --> D\n    I -- No --> K --> L\n```\n\nOnly `messages` drives the loop: model output and tool results are appended, then the next request is rebuilt from that updated history.\n\n## 2. What `build_turn_context()` does\n\nCalled at `agent/conversation_loop.py:L592–622`, implemented at `agent/turn_context.py:L119–562`:\n\n```text\nconversation_history\n  → shallow-copy into messages                    L271–272\n  → restore todo and periodic counters             L274–314\n  → append the current user message                L316–320\n  → restore/build _cached_system_prompt             L329–333\n  → persist inbound user early                     L335–347\n  → run preflight compression when needed          L350–460\n  → fetch plugin context / external memory recall\n  → return TurnContext                             L553–562\n```\n\nIt returns loop state, not the final provider request.\n\n## 3. How `messages` becomes a request\n\nEach loop iteration projects the request again (`agent/conversation_loop.py:L787–954`):\n\n```text\nmessages\n  → copy each row into api_messages\n  → append external memory/plugin context only to the current user copy\n  → prepend active_system_prompt + ephemeral_system_prompt\n  → insert optional prefill\n  → send together with tools\n```\n\nDynamic recall and plugin context do not mutate the user's stored message. The system prompt is normally prepended at request time rather than stored in `messages`.\n\n## 4. Three compression checks\n\n1. **At turn start:** `turn_context.py:L350–460`.\n2. **Before an API request:** `conversation_loop.py:L954–1058`; compressed `messages` replaces the temporary request.\n3. **After tool execution:** `conversation_loop.py:L4733–4778`, using real provider prompt usage when available.\n\n### Compression call chain\n\n```text\nAIAgent._compress_context()                       run_agent.py:L5608\n  → conversation_compression.compress_context()  conversation_compression.py:L435\n  → agent.context_compressor.compress()           conversation_compression.py:L638\n  → ContextCompressor.compress()                  context_compressor.py:L2793\n```\n\n### Five-stage algorithm\n\n```mermaid\nflowchart LR\n    A[\"_prune_old_tool_results()<br/>Drop older large tool outputs<br/>L2852–2858\"] -->\n    B[\"_protect_head_size()<br/>Keep the opening conversation anchors<br/>L2860–2863\"] -->\n    C[\"_find_tail_cut_by_tokens()<br/>Keep recent messages within a token budget<br/>L2864–2885\"] -->\n    D[\"_generate_summary()<br/>Send old summary, middle, and prompt to the LLM<br/>L2886–2935\"] -->\n    E[\"Reassemble transcript<br/>head + summary + tail<br/>L2997+\"]\n```\n\nAn existing handoff summary is updated in the same summary request. The result replaces canonical `messages`; compression is not limited to a temporary API copy.\n\n## 5. How model results return to `messages`\n\nWith tool calls:\n\n```text\nassistant(tool_calls) → messages                conversation_loop.py:L4660\npersist the tool-call block before side effects  L4662–4667\nagent._execute_tool_calls(...)                   L4688\ntool results → the same messages                 tool_executor.py:L918–962\noptional compression                             L4733–4778\ncontinue the Agent loop                          L4780–4784\n```\n\nWithout tool calls, the final assistant message is appended and the loop exits at `L4786–5234`.\n\n## 6. `finalize_turn()`\n\nAll exits converge at `agent/turn_finalizer.py:L30`. It closes a valid transcript, persists `messages`, builds the result dictionary, syncs external memory, and may launch the Chapter 3 background review.\n\n```text\nresult[\"messages\"]\n  → caller / SessionDB\n  → next conversation_history\n  → build_turn_context()\n  → new messages\n```\n", "fileEn": "en/02-execution-loop.md"}, {"num": "③", "prefix": "03", "title": "状态、Memory 与自进化", "titleEn": "Memory and Self-Improvement", "phase": "运行机制", "phaseEn": "Runtime", "summary": "前台与后台 Agent 如何更新 Memory，以及持久状态怎样进入后续上下文。", "summaryEn": "How foreground and review agents update durable knowledge and inject it into later context.", "codeAnchors": [{"title": "Memory 周期触发", "path": "agent/turn_context.py", "start": 278, "end": 314, "note": "从持久历史恢复计数，并按用户 turn 设置 memory review bool。", "range": "agent/turn_context.py:L278-L314", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/agent/turn_context.py#L278-L314"}, {"title": "Finalizer 合并两个触发器", "path": "agent/turn_finalizer.py", "start": 454, "end": 480, "note": "Skill 按 loop iteration 触发；任一 trigger 成立即启动后台 review。", "range": "agent/turn_finalizer.py:L454-L480", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/agent/turn_finalizer.py#L454-L480"}, {"title": "构造隔离 Review Agent", "path": "agent/background_review.py", "start": 650, "end": 830, "note": "继承运行时和会话快照，但关闭持久化、压缩和递归 review，只开放 memory/skill。", "range": "agent/background_review.py:L650-L830", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/agent/background_review.py#L650-L830"}, {"title": "统一 Memory 写入口", "path": "tools/memory_tool.py", "start": 959, "end": 1034, "note": "前台和后台最终都通过 add/replace/remove/batch 修改 MemoryStore。", "range": "tools/memory_tool.py:L959-L1034", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/tools/memory_tool.py#L959-L1034"}, {"title": "Memory 冻结快照", "path": "tools/memory_tool.py", "start": 169, "end": 204, "note": "live entries 可以写盘，但当前 session 的 system-prompt snapshot 保持不变。", "range": "tools/memory_tool.py:L169-L204", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/tools/memory_tool.py#L169-L204"}, {"title": "Memory 进入 system prompt", "path": "agent/system_prompt.py", "start": 450, "end": 482, "note": "未来 fresh Agent 从冻结 snapshot 构造 cached system prompt。", "range": "agent/system_prompt.py:L450-L482", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/agent/system_prompt.py#L450-L482"}, {"title": "Curator consolidation", "path": "agent/curator.py", "start": 1480, "end": 1590, "note": "确定性审计默认运行；LLM umbrella consolidation 必须显式开启。", "range": "agent/curator.py:L1480-L1590", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/agent/curator.py#L1480-L1590"}], "raw": "# Memory 与 Skill 怎样自我更新\n\nHermes 的自改进不是另一套运行时。它只是周期性启动一个受限的 `AIAgent`，重放会话并让它通过普通工具整理 Memory 或 Skills。\n\n## 1. 两种写入路径\n\n```mermaid\nflowchart TD\n    A[\"前台 run_conversation()<br/>模型可在正常任务中通过工具主动沉淀经验\"]\n    B[\"finalize_turn()<br/>根据 memory/skill 计数决定是否复盘\"]\n    C[\"Review Agent.run_conversation()<br/>用（工具）受限的 Agent 专门检查会话快照\"]\n    D[\"memory() / skill_manage()<br/>统一执行长期知识的增删改\"]\n    E[\"MEMORY.md · USER.md · Skills<br/>保存跨会话可复用状态\"]\n    F[\"注入到以后 session 的 system prompt\"]\n\n    A -->|模型主动判断值得保存| D\n    B --> C -->|专门复盘会话| D\n    D --> E\n    E --> F\n```\n\n无论前台还是后台，最终都必须调用工具；review 代码不会绕过工具直接改文件。\n\n## 2. 两个周期触发器\n\n### Memory：按用户 turn 计数\n\n`build_turn_context()` 每接收一次用户 turn，就递增 `_turns_since_memory`。达到 `_memory_nudge_interval` 后设置 `should_review_memory=True`，并把计数清零：`agent/turn_context.py:L278–314`。\n\n### Skill：按 Agent loop iteration 计数\n\n每进入一次模型循环，`_iters_since_skill` 加一：`agent/conversation_loop.py:L680–684`。它统计的是 API/Agent iteration，不严格等于实际 tool call 数。`finalize_turn()` 在达到 `_skill_nudge_interval` 且 `skill_manage` 可用时设置 skill review：`agent/turn_finalizer.py:L454–460`。\n\n最后只有在本轮存在最终回复、没有中断，并且至少一个 trigger 成立时，才启动后台 review：`agent/turn_finalizer.py:L470–480`。\n\n## 3. Background Review 主链\n\n```mermaid\nflowchart TD\n    A[\"finalize_turn()<br/>汇总 memory / skill 触发条件\"]\n    B[\"_spawn_background_review()<br/>创建 thread，不阻塞前台回复<br/>run_agent.py:L1598\"]\n    C[\"spawn_background_review_thread()<br/>选择用于 review memory/skill/combined 的 prompt<br/>background_review.py:L925–950\"]\n    D[\"_run_review_in_thread()<br/>在后台构造并运行受限 Review Agent<br/>L607\"]\n    E[\"AIAgent(...)<br/>继承模型运行时，但隔离持久化和压缩<br/>L650–776\"]\n    F[\"review_agent.run_conversation()<br/>把 snapshot 当历史、review prompt 当新 user prompt<br/>L812–830\"]\n    G[\"memory() / skill_manage()<br/>由 Review Agent 自主选择工具进行需要的持久化操作\"]\n\n    A --> B --> C --> D --> E --> F --> G\n```\n\n`_run_review_in_thread()` 的输入只有父 Agent、`messages_snapshot` 和选好的 prompt。它的核心步骤是：\n\n1. 继承父 Agent 当前的 provider、model、credentials 和 toolset 配置；\n2. 创建 `max_iterations=16` 的新 `AIAgent`；\n3. 绑定父 Agent 的内建 `MemoryStore`；\n4. 用线程级白名单把实际执行权限限制为 memory/skill 工具；\n5. 把父会话 snapshot 作为 `conversation_history`，把 review prompt 作为新的 user message，再次调用 `run_conversation()`；\n6. 扫描本次新增的成功 tool results，向前台汇报做了哪些更新。\n\n同模型时使用完整 snapshot 和父 Agent 的 cached system prompt，以复用 prompt cache；配置了不同的 background-review 模型时，旧历史会先变成 digest，再交给新模型：`agent/background_review.py:L734–819`。\n\n## 4. 为什么 review 不会污染主会话\n\nReview Agent 复用核心，但隔离生命周期：\n\n| 设置 | 作用 | 源码 |\n|---|---|---|\n| `skip_memory=True` | 不把 review prompt 写入外部 memory provider | `background_review.py:L665–697` |\n| `_persist_disabled=True` | 不把 review 对话写入父 SessionDB | `L712–725` |\n| nudge interval = 0 | 不递归触发另一个 review | `L707–711` |\n| `compression_enabled=False` | 不与父会话争抢压缩和 session rotation | `L766–776` |\n| thread tool whitelist | 只允许 memory/skill 工具真正执行 | `L778–830` |\n\n因此，review 的自然语言对话不会并回主 `messages`；只有工具造成的持久状态变化会留下来。\n\n## 5. Memory 怎样写入和再次注入\n\n前台 Agent 和 Review Agent 最终汇合到同一个入口：`tools/memory_tool.py:L959–1034`。\n\n```text\nmemory(add / replace / remove / batch)\n  → MemoryStore\n  → $HERMES_HOME/memories/MEMORY.md 或 USER.md\n```\n\n- `MEMORY.md`：项目、环境、工具和长期经验。\n- `USER.md`：用户偏好、习惯和稳定工作方式。\n\nAgent 初始化时，`MemoryStore.load_from_disk()` 读取文件并生成用于 system prompt 的冻结 snapshot：`tools/memory_tool.py:L169–204`。构造 system prompt 时将它注入 memory/user 区域：`agent/system_prompt.py:L450–482`。\n\n```text\nMEMORY.md / USER.md\n  → fresh AIAgent / 新 session 加载\n  → frozen memory snapshot\n  → build_system_prompt()\n  → _cached_system_prompt\n  → 后续模型请求\n```\n\n当前 session 内调用 memory 工具会立即更新文件和 live entries，但不会修改已经冻结的 snapshot，也不会改写本轮 `_cached_system_prompt`。新记忆通常从未来的新 session/fresh Agent 开始进入 system prompt。这是为了保持 prompt cache 前缀稳定。\n\n外部 memory provider 是另一条链：每个 turn 开始时 recall，并只注入当前 `api_messages` 的 user 副本；turn 结束后同步完整 user/assistant turn。核心锚点分别是 `agent/turn_context.py:L536–551`、`agent/conversation_loop.py:L787–808` 和 `agent/turn_finalizer.py:L462–468`。\n\n## 6. Skill 怎样写入和生效\n\n前台或 Review Agent 调用 `skill_manage` 创建、修改或归档 agent-created skills。工具完成文件写入并使技能索引缓存失效；以后构造 system prompt 时只放入 skill 的名称和描述，模型真正需要时再通过 `skills_list()` / `skill_view()` 加载完整 `SKILL.md`。\n\n```text\nskill_manage\n  → skill files\n  → skill index cache 失效\n  → 未来 system prompt 出现 skill 索引\n  → 按需加载完整 SKILL.md\n```\n\n周期触发的 background review 负责从近期对话中提炼或修正 skill；独立 curator 负责更长期的技能治理。`agent/curator.py:L1480` 的 `run_curator_review()` 默认执行确定性审计；只有配置 `curator.consolidate: true` 或显式传入 `--consolidate`，才启动 LLM consolidation，把相关 skills 合并为 umbrella skill。\n\n## 7. 一句话总结\n\n```text\n前台任务或周期 review 发现可复用经验\n  → 仍通过 memory / skill_manage 工具写盘\n  → 不改当前会话的 cached system prompt\n  → 在未来 session 的 system prompt 或按需 skill 加载中生效\n```\n", "available": true, "file": "03-状态与自改进.md", "rawEn": "# How Memory and Skills Improve Themselves\n\nSelf-improvement is not a separate runtime. Hermes periodically starts a restricted `AIAgent`, replays the conversation, and lets that agent organize Memory or Skills through normal tools.\n\n## 1. Two write paths\n\n```mermaid\nflowchart TD\n    A[\"Foreground run_conversation()<br/>The model may save useful experience during normal work\"]\n    B[\"finalize_turn()<br/>Use memory/skill counters to decide whether to review\"]\n    C[\"Review Agent.run_conversation()<br/>Inspect a snapshot with a restricted Agent\"]\n    D[\"memory() / skill_manage()<br/>Apply durable knowledge changes\"]\n    E[\"MEMORY.md · USER.md · Skills<br/>Store reusable cross-session state\"]\n\n    A -->|Model decides to save| D\n    B --> C -->|Dedicated review| D\n    D --> E\n```\n\nBoth paths must use tools. Review code does not edit these files directly.\n\n## 2. Two periodic triggers\n\n### Memory counts user turns\n\n`build_turn_context()` increments `_turns_since_memory`. At `_memory_nudge_interval`, it sets `should_review_memory=True` and resets the counter (`agent/turn_context.py:L278–314`).\n\n### Skills count Agent-loop iterations\n\nEvery model-loop iteration increments `_iters_since_skill` (`agent/conversation_loop.py:L680–684`). This is an API/Agent iteration count, not exactly a tool-call count. `finalize_turn()` triggers a review when the interval is reached and `skill_manage` is available (`agent/turn_finalizer.py:L454–460`).\n\nA background review starts only when the turn has a final response, was not interrupted, and at least one trigger fired (`L470–480`).\n\n## 3. Background Review chain\n\n```mermaid\nflowchart TD\n    A[\"finalize_turn()<br/>Combine memory and skill trigger flags\"]\n    B[\"_spawn_background_review()<br/>Start a thread without blocking the foreground reply<br/>run_agent.py:L1598\"]\n    C[\"spawn_background_review_thread()<br/>Choose memory, skill, or combined prompt<br/>background_review.py:L925–950\"]\n    D[\"_run_review_in_thread()<br/>Build and run a restricted Review Agent<br/>L607\"]\n    E[\"AIAgent(...)<br/>Inherit model runtime; isolate persistence and compression<br/>L650–776\"]\n    F[\"review_agent.run_conversation()<br/>snapshot as history, review prompt as new user<br/>L812–830\"]\n    G[\"memory() / skill_manage()<br/>The Review Agent selects durable mutations\"]\n\n    A --> B --> C --> D --> E --> F --> G\n```\n\n`_run_review_in_thread()` inherits the live provider, model, credentials, and toolset configuration; creates a new `AIAgent(max_iterations=16)`; binds the parent's built-in `MemoryStore`; restricts dispatch to memory/skill tools; then runs `run_conversation()` over the snapshot.\n\nThe same model reuses the full snapshot and cached system prompt. A separately configured review model receives a digest of older history (`agent/background_review.py:L734–819`).\n\n## 4. Why review does not contaminate the main session\n\n| Setting | Purpose | Source |\n|---|---|---|\n| `skip_memory=True` | Do not ingest review prompts into external memory providers | `background_review.py:L665–697` |\n| `_persist_disabled=True` | Do not write review dialogue into the parent SessionDB | `L712–725` |\n| nudge intervals = 0 | Prevent recursive reviews | `L707–711` |\n| `compression_enabled=False` | Avoid compression/session-rotation races | `L766–776` |\n| thread tool whitelist | Execute only memory/skill tools | `L778–830` |\n\nReview dialogue never merges into the main `messages`; only tool-driven durable changes survive.\n\n## 5. How Memory is written and injected\n\nForeground and Review agents share `tools/memory_tool.py:L959–1034`:\n\n```text\nmemory(add / replace / remove / batch)\n  → MemoryStore\n  → $HERMES_HOME/memories/MEMORY.md or USER.md\n```\n\n`MEMORY.md` stores project, environment, tool, and durable operational facts. `USER.md` stores user preferences and stable working habits.\n\nAt Agent initialization, `MemoryStore.load_from_disk()` creates a frozen system-prompt snapshot (`tools/memory_tool.py:L169–204`), injected at `agent/system_prompt.py:L450–482`:\n\n```text\nMEMORY.md / USER.md\n  → fresh AIAgent / new session\n  → frozen memory snapshot\n  → build_system_prompt()\n  → _cached_system_prompt\n```\n\nA write updates disk and live entries immediately, but not the current session's frozen snapshot or cached system prompt. New memory normally enters a future fresh session, preserving the prompt-cache prefix.\n\nExternal memory providers are separate: recall occurs at turn start and is injected only into the current `api_messages` user copy; completed user/assistant turns sync at turn end.\n\n## 6. How Skills are written and loaded\n\nForeground or Review agents use `skill_manage` to create, modify, or archive agent-created skills. The system prompt contains only the skill name and description; full `SKILL.md` content is loaded on demand through `skills_list()` / `skill_view()`.\n\n```text\nskill_manage\n  → skill files\n  → invalidate skill index cache\n  → future system prompt contains the skill index\n  → load full SKILL.md only when needed\n```\n\nBackground review extracts skills from recent conversations. The separate curator performs longer-term governance. `run_curator_review()` (`agent/curator.py:L1480`) performs deterministic auditing by default; LLM umbrella consolidation requires `curator.consolidate: true` or `--consolidate`.\n\n## 7. Summary\n\n```text\nforeground work or periodic review finds reusable experience\n  → memory / skill_manage writes it to disk\n  → current cached system prompt remains unchanged\n  → future sessions or on-demand skill loading make it available\n```\n", "fileEn": "en/03-memory-self-improvement.md"}, {"num": "④", "prefix": "04", "title": "工具、代码与子代理", "titleEn": "Tools, Code, and Subagents", "phase": "运行机制", "phaseEn": "Runtime", "summary": "直接工具、execute_code 与 delegate_task 的编排和隔离边界。", "summaryEn": "The orchestration and isolation boundaries of direct tools, execute_code, and delegate_task.", "codeAnchors": [{"title": "execute_code 编排工具", "path": "tools/code_execution_tool.py", "start": 1115, "end": 1170, "note": "确定性的循环和过滤留在脚本中，主会话只接收有界结果。", "range": "tools/code_execution_tool.py:L1115-L1170", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/tools/code_execution_tool.py#L1115-L1170"}, {"title": "委派运行方式", "path": "run_agent.py", "start": 5684, "end": 5714, "note": "顶层委派强制后台运行；orchestrator 子代理同步等待 worker。", "range": "run_agent.py:L5684-L5714", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/run_agent.py#L5684-L5714"}, {"title": "角色与工具继承", "path": "tools/delegate_tool.py", "start": 1078, "end": 1152, "note": "默认 leaf 降权；orchestrator 只有在配置和深度允许时保留 delegation。", "range": "tools/delegate_tool.py:L1078-L1152", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/tools/delegate_tool.py#L1078-L1152"}, {"title": "构造独立 Child Agent", "path": "tools/delegate_tool.py", "start": 1301, "end": 1354, "note": "创建独立 session、预算和 prompt，同时记录父子身份。", "range": "tools/delegate_tool.py:L1301-L1354", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/tools/delegate_tool.py#L1301-L1354"}, {"title": "启动 Child Loop", "path": "tools/delegate_tool.py", "start": 1919, "end": 1925, "note": "只传 goal 和 child task_id，不复制父 conversation_history。", "range": "tools/delegate_tool.py:L1919-L1925", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/tools/delegate_tool.py#L1919-L1925"}, {"title": "后台结果回流", "path": "tools/async_delegation.py", "start": 249, "end": 319, "note": "Child 结果进入共享 completion queue，再按原 session 投递为新 turn。", "range": "tools/async_delegation.py:L249-L319", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/tools/async_delegation.py#L249-L319"}], "raw": "# 工具、代码与子代理\n\nHermes 有三种主要执行方式。区别不在“能力强弱”，而在工作由谁编排、多少中间结果进入主会话。\n\n| 方式 | 谁编排步骤 | 主 `messages` 看到什么 | 适合场景 |\n|---|---|---|---|\n| 直接工具 | 主 Agent | 每次 tool call 和 result | 单次读取、命令、搜索、写文件 |\n| `execute_code` | 一段 Python 代码 | 最终 stdout、状态和错误 | 确定性的循环、过滤、批处理 |\n| `delegate_task` | 独立子 Agent | dispatch handle 和最终总结 | 可拆分的研究、审查、调试 |\n\nCron 和后台 terminal 用于定时或长时间运行，不属于子代理机制。\n\n## 1. 直接工具\n\n直接工具沿用第 2 章的闭环：\n\n```text\n模型返回 tool_calls\n  → assistant(tool_calls) 写入 messages\n  → ToolExecutor 调用工具\n  → tool results 写入 messages\n  → 下一轮模型请求\n```\n\n入口是 `agent/conversation_loop.py:L4660–4688`，结果回填在 `agent/tool_executor.py:L918–962`。这种方式最透明，但每个中间结果都会占用主会话 context。\n\n## 2. `execute_code`\n\n`execute_code` 让模型生成一段 Python，用 Hermes RPC 在脚本内部调用已有工具：`tools/code_execution_tool.py:L1115–1170`。\n\n```text\n主 Agent\n  → execute_code(python script)\n  → script 内循环调用多个 Hermes tools\n  → 只把有界 stdout / 状态 / 错误返回主 messages\n```\n\n它适合“不需要模型逐步判断”的机械流程，例如批量读取、过滤结果再统一处理。若每一步都需要根据语义重新决策，应使用直接工具或子代理。\n\n## 3. `delegate_task` 的核心链路\n\n```mermaid\nflowchart TD\n    A[\"delegate_task()<br/>父 Agent 调用这个 tool，写出多个独立目标交给子代理\"]\n    B[\"_dispatch_delegate_task()<br/>决定顶层后台运行或嵌套同步等待<br/>run_agent.py:L5684–5714\"]\n    C[\"delegate_task()<br/>校验任务、深度、角色和并发限制<br/>delegate_tool.py:L2342 起\"]\n    D[\"_build_child_agent()<br/>派生工具和 prompt，实例化独立 AIAgent<br/>L1044–1397\"]\n    E[\"Child AIAgent<br/>拥有独立 session、messages 和预算\"]\n    F[\"child.run_conversation(goal)<br/>只以主 agent 写的 goal 启动新的 conversation loop（干净的上下文）<br/>L1919–1925\"]\n    G[\"Child Agent loop<br/>独立调用模型和工具完成目标\"]\n    H[\"结果回传<br/>成为 delegate_task tool result\"]\n\n    A --> B --> C --> D --> E --> F --> G --> H\n```\n\n### 顶层与嵌套委派\n\n`AIAgent._dispatch_delegate_task()` 决定运行方式：`run_agent.py:L5684–5714`。\n\n- 顶层 Agent 发起的委派强制后台运行，模型不能自行要求同步等待。\n- orchestrator 子代理发起的委派保持同步，因为它必须拿到 workers 的结果，才能给父 Agent 汇总。\n\nsingle 和 batch 都走同一入口；batch 只是在受控线程池中并行运行多个 child：`tools/delegate_tool.py:L2342–2554`。\n\n## 4. Child Agent 怎样构造\n\n核心函数是 `tools/delegate_tool.py:L1044–1397` 的 `_build_child_agent()`。\n\n```text\ngoal + context\n  → 构造 child system prompt\n父 Agent 的运行配置\n  → 派生 model / provider / credentials / tools\nnew AIAgent(...)\n  → 独立 session、独立预算、独立 conversation loop\n```\n\n| 项目 | Child 的行为 | 源码 |\n|---|---|---|\n| `goal` | 进入 child prompt，并作为第一条 user message | `L1144–1152, L1921–1925` |\n| `context` | 进入 child prompt | `L1144–1152` |\n| 父会话历史 | **不复制** | `L1921–1925` 没有传 `conversation_history` |\n| 模型与凭证 | 默认继承父 Agent，也可由可信配置改路由 | `L1196–1214` |\n| 工具 | 从父 Agent 的有效工具集派生，不能凭空扩权 | `L1100–1142` |\n| session | 新 session，同时记录 `parent_session_id` | `L1301–1354` |\n| 预算 | 每个 child 创建新的 iteration budget | `L1178–1181, L1309–1331` |\n| Memory/context files | 不重新加载，由 `goal/context` 显式提供背景 | `L1316–1321` |\n\n最重要的结论是：子代理看不到父 Agent 的 `messages`。父 Agent 必须把路径、错误、已尝试方案、约束和验收标准写进 `goal/context`。\n\n## 5. Leaf 与 Orchestrator\n\n默认 child 是 `leaf`。构造时会移除以下高风险或不适合独立 worker 的能力：\n\n```text\ndelegate_task · clarify · memory · send_message · execute_code · cronjob\n```\n\n阻止列表在 `tools/delegate_tool.py:L44–54`，角色和工具继承在 `L1078–1142`。\n\n`role=\"orchestrator\"` 只有在配置允许、并且没有达到 `max_spawn_depth` 时才保留 `delegate_task`。默认最大深度是 1，因此通常只有：\n\n```text\nParent Agent\n  └─ Leaf Child\n```\n\n开启更深层级后才可能出现：\n\n```text\nParent Agent\n  └─ Orchestrator Child\n       └─ Worker Child\n```\n\n## 6. 结果怎样回到父会话\n\n### 同步路径\n\norchestrator child 等待 worker 完成。worker 的汇总作为 `delegate_task` tool result 写入 orchestrator 自己的 `messages`，然后 orchestrator 继续 loop 并生成最终总结。\n\n### 后台路径\n\n顶层 Agent 先得到 dispatched handle，当前 tool loop 不必等待。child 完成后，结果写入共享 completion queue：`tools/async_delegation.py:L249–319`。CLI、Gateway 或 TUI 再按原 session 把完成事件投递为一个新的 turn。\n\n```text\n父 turn：delegate_task → dispatched handle → 继续/结束\n\nchild 完成\n  → completion_queue\n  → 原 session 的新 turn\n  → 父 Agent 阅读结果并继续处理\n```\n\n结果不会插入已经结束的旧 `messages` 中。这样可以保持严格的消息角色顺序，也不会改写已经缓存的历史前缀。\n\n## 7. 怎样选择\n\n```text\n一次简单动作                    → 直接工具\n确定性的循环、过滤、批处理       → execute_code\n需要独立推理且上下文可以明确描述 → delegate_task\n需要定时执行                    → Cron\n需要等待长时间 shell 进程        → terminal(background=True)\n```\n\n子代理的总结仍是模型输出。涉及文件、外部资源或其他副作用时，父 Agent 应通过文件回读、状态查询、URL 或资源 ID 再次验证。\n", "available": true, "file": "04-工具代码与子代理.md", "rawEn": "# Tools, Code, and Subagents\n\nHermes has three main execution styles. The difference is who orchestrates the steps and how much intermediate data enters the main conversation.\n\n| Style | Orchestrator | What main `messages` receives | Best for |\n|---|---|---|---|\n| Direct tools | Main Agent | Every call and result | One read, command, search, or write |\n| `execute_code` | Python program | Bounded stdout, status, errors | Deterministic loops and batch processing |\n| `delegate_task` | Independent Child Agent | Dispatch handle and final summary | Separable research, review, debugging |\n\nCron and background terminal processes handle scheduled or long-running work; they are not subagents.\n\n## 1. Direct tools\n\nDirect tools use the Chapter 2 loop:\n\n```text\nmodel returns tool_calls\n  → assistant(tool_calls) enters messages\n  → ToolExecutor runs the tool\n  → tool results enter messages\n  → next model request\n```\n\nThe path starts at `agent/conversation_loop.py:L4660–4688`; results are appended at `agent/tool_executor.py:L918–962`. It is transparent, but every intermediate result consumes main-context space.\n\n## 2. `execute_code`\n\n`execute_code` runs model-authored Python that invokes existing Hermes tools through RPC (`tools/code_execution_tool.py:L1115–1170`).\n\n```text\nmain Agent\n  → execute_code(Python script)\n  → script loops over Hermes tools internally\n  → bounded stdout / status / errors return to main messages\n```\n\nUse it when the intermediate decisions are mechanical. Use direct tools or subagents when each step requires new semantic judgment.\n\n## 3. Core `delegate_task` chain\n\n```mermaid\nflowchart TD\n    A[\"delegate_task()<br/>Give independent goals to Child Agents\"]\n    B[\"_dispatch_delegate_task()<br/>Top-level background or nested synchronous execution<br/>run_agent.py:L5684–5714\"]\n    C[\"delegate_task()<br/>Validate tasks, depth, role, and concurrency<br/>delegate_tool.py:L2342+\"]\n    D[\"_build_child_agent()<br/>Derive tools and prompt; construct an independent AIAgent<br/>L1044–1397\"]\n    E[\"Child AIAgent<br/>Independent session, messages, and budget\"]\n    F[\"child.run_conversation(goal)<br/>Start a fresh loop with only the goal<br/>L1919–1925\"]\n    G[\"Child Agent loop<br/>Use its own model and tools to finish the goal\"]\n    H[\"Return result<br/>Tool result when synchronous; new turn when asynchronous\"]\n\n    A --> B --> C --> D --> E --> F --> G --> H\n```\n\n### Top-level and nested delegation\n\n`AIAgent._dispatch_delegate_task()` (`run_agent.py:L5684–5714`) forces top-level delegation into the background. An orchestrator child delegates synchronously because it needs worker results before composing its summary.\n\nSingle and batch forms share the same entry point; batch runs children in a bounded thread pool (`tools/delegate_tool.py:L2342–2554`).\n\n## 4. How a Child Agent is constructed\n\n`_build_child_agent()` lives at `tools/delegate_tool.py:L1044–1397`.\n\n| Item | Child behavior | Source |\n|---|---|---|\n| `goal` | Enters child prompt and becomes the first user message | `L1144–1152, L1921–1925` |\n| `context` | Enters child prompt | `L1144–1152` |\n| Parent transcript | **Not copied** | `L1921–1925` passes no `conversation_history` |\n| Model/credentials | Inherit by default; trusted config may reroute | `L1196–1214` |\n| Tools | Derived from the parent's effective tools; cannot gain privileges | `L1100–1142` |\n| Session | New session with `parent_session_id` | `L1301–1354` |\n| Budget | Fresh iteration budget per child | `L1178–1181, L1309–1331` |\n| Memory/context files | Not reloaded; required background must be explicit | `L1316–1321` |\n\nThe child cannot see the parent's `messages`. Paths, errors, prior attempts, constraints, and acceptance criteria must be included in `goal/context`.\n\n## 5. Leaf and Orchestrator\n\nA child is a `leaf` by default. Construction removes `delegate_task`, `clarify`, `memory`, `send_message`, `execute_code`, and `cronjob` (`tools/delegate_tool.py:L44–54`).\n\n`role=\"orchestrator\"` keeps delegation only when configuration and `max_spawn_depth` allow it. The default maximum depth is 1:\n\n```text\nParent Agent\n  └─ Leaf Child\n```\n\nWith deeper delegation enabled:\n\n```text\nParent Agent\n  └─ Orchestrator Child\n       └─ Worker Child\n```\n\n## 6. How results return\n\nFor synchronous nested delegation, the worker summary becomes a `delegate_task` tool result in the orchestrator's `messages`.\n\nFor top-level background delegation, the parent first receives a dispatch handle. Completion enters the shared queue (`tools/async_delegation.py:L249–319`) and the host delivers it to the original session as a new turn.\n\n```text\nparent turn: delegate_task → dispatch handle → continue/end\n\nchild completes\n  → completion_queue\n  → new turn in the original session\n  → parent Agent reads and acts on the result\n```\n\nThe result is never inserted into an already-finished old transcript, preserving role order and the cached prefix.\n\n## 7. Choosing a path\n\n```text\none simple action                       → direct tool\ndeterministic loop/filter/batch         → execute_code\nindependent reasoning with clear input  → delegate_task\nscheduled execution                     → Cron\nlong-running shell process              → terminal(background=True)\n```\n\nSubagent summaries are still model output. Verify file changes and external side effects through reads, status queries, URLs, or resource IDs.\n", "fileEn": "en/04-tools-code-subagents.md"}, {"num": "⑤", "prefix": "05", "title": "会话存储与搜索", "titleEn": "Session Storage and Search", "phase": "状态与上下文", "phaseEn": "State and Context", "summary": "SQLite、FTS5、原位压缩和 session_search 如何配合。", "summaryEn": "How SQLite, FTS5, in-place compaction, and session_search work together.", "codeAnchors": [{"title": "SessionDB schema", "path": "hermes_state.py", "start": 730, "end": 870, "note": "sessions、messages 与两套 FTS 索引组成持久会话的主体。", "range": "hermes_state.py:L730-L870", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/hermes_state.py#L730-L870"}, {"title": "增量写入 messages", "path": "run_agent.py", "start": 1753, "end": 1910, "note": "以 conversation_history 为 baseline，只追加尚未持久化的消息。", "range": "run_agent.py:L1753-L1910", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/run_agent.py#L1753-L1910"}, {"title": "恢复 active transcript", "path": "hermes_state.py", "start": 4084, "end": 4191, "note": "把 active rows 还原成下一轮的 conversation_history。", "range": "hermes_state.py:L4084-L4191", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/hermes_state.py#L4084-L4191"}, {"title": "原位压缩", "path": "hermes_state.py", "start": 3694, "end": 3744, "note": "旧 rows 标记 compacted，新 head + summary + tail 成为 active view。", "range": "hermes_state.py:L3694-L3744", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/hermes_state.py#L3694-L3744"}, {"title": "全文搜索", "path": "hermes_state.py", "start": 4512, "end": 4705, "note": "普通 FTS 与 trigram 共同覆盖词项、中文和子串搜索。", "range": "hermes_state.py:L4512-L4705", "url": "https://github.com/NousResearch/hermes-agent/blob/540f90190f50f9518bf36632a724e0e58877a10b/hermes_state.py#L4512-L4705"}], "raw": "# 会话怎样持久化和搜索\n\nHermes 把交互会话写入 `$HERMES_HOME/state.db`。理解这一章只需要区分三份内存状态和一份持久状态。\n\n| 状态 | 含义 |\n|---|---|\n| `conversation_history` | 本轮开始时已有的历史，也是增量写入的 baseline |\n| `messages` | 本轮 loop 正在修改的 canonical transcript |\n| `agent._session_messages` | 长生命周期 Agent 保存的最新 `messages` |\n| SessionDB | 跨进程、跨重启保存的 durable rows |\n\n## 1. SessionDB 存什么\n\n核心表定义在 `hermes_state.py:L730–870`：\n\n```text\nstate.db\n├── sessions              session 元数据、system prompt、模型、用量和状态\n├── messages              user / assistant / tool、reasoning、tool calls/results\n├── messages_fts          普通全文索引\n├── messages_fts_trigram  中文和子串搜索索引\n├── gateway_routing       消息平台到 session 的路由\n└── compression_locks     跨进程压缩锁\n```\n\n它会保存完整的会话消息，但“完整”不等于所有消息都继续发给模型。每条 message 可以是 active、compacted 或 inactive；正常恢复只读取 active rows，压缩过的旧 rows 仍可留在数据库中供搜索。\n\n## 2. 一次 turn 怎样写入\n\n```mermaid\nflowchart TD\n    A[\"get_messages_as_conversation()<br/>从 SessionDB active rows 恢复会话\"]\n    B[\"conversation_history<br/>作为本轮增量写入的 baseline\"]\n    C[\"build_turn_context()<br/>浅复制 history，形成可修改的 messages<br/>turn_context.py:L271–272\"]\n    D[\"Agent loop<br/>持续追加 user、assistant 和 tool results\"]\n    E[\"_flush_messages_to_session_db()<br/>向 SessionDB 追加 baseline 之后的新消息<br/>run_agent.py:L1753\"]\n    F[\"agent._session_messages<br/>保存长生命周期 Agent 的最新 transcript\"]\n    G[\"finalize_turn()<br/>再次持久化并保证 turn 正常闭合<br/>turn_finalizer.py:L185–207\"]\n    H[\"下一轮 conversation_history<br/>继续使用 live state 或重新从 DB 恢复\"]\n\n    A --> B --> C --> D --> E --> F --> G --> H\n```\n\n关键变化是：\n\n```text\nconversation_history = [旧 active messages]\n\nmessages = list(conversation_history)\nmessages += current user\nmessages += assistant(tool_calls)\nmessages += tool results\nmessages += final assistant\n\nagent._session_messages = messages\nSessionDB += 尚未写入的新 messages\n```\n\n`_flush_messages_to_session_db()` 位于 `run_agent.py:L1753–1910`。它使用 `conversation_history` baseline 和 message 上的持久化标记，只追加新增部分，不在每次 tool call 后重写整段历史。\n\nTurn 开始时也会提前保存 inbound user：`agent/turn_context.py:L335–347`。因此即使进程在工具执行中途退出，用户输入和已经发生的 tool-call block 仍有机会恢复。\n\n## 3. 下一轮从哪里恢复\n\nSessionDB 的恢复入口是 `hermes_state.py:L4084–4191` 的 `get_messages_as_conversation()`。它把 active message rows 还原成模型可消费的字典，包括 reasoning 和 tool-call/result 关系。\n\n```text\n同一个长生命周期 Agent\n  → 优先继续使用 agent._session_messages\n\n进程重启 / resume\n  → SessionDB.get_messages_as_conversation()\n  → conversation_history\n  → build_turn_context()\n  → messages\n```\n\nsession row 中保存的 system prompt 也会原样恢复，而不是根据当前文件和 Memory 重新构造。这保证 resumed session 的 prompt-cache 前缀保持稳定。\n\n## 4. 压缩后数据库发生什么\n\n默认使用原位压缩。核心事务是 `hermes_state.py:L3694–3744` 的 `archive_and_compact()`：\n\n```text\n压缩前 active rows\n  → active=0, compacted=1\n\nhead + handoff summary + recent tail\n  → 写成新的 active rows\n\nconversation_history\n  → 重设为 compressed messages baseline\n```\n\n```mermaid\nflowchart LR\n    A[\"完整 active messages<br/>当前模型正在使用的 transcript\"] --> B[\"ContextCompressor.compress()<br/>把中间历史压成 handoff summary\"]\n    B --> C[\"head + summary + tail<br/>形成新的 active transcript\"]\n    A --> D[\"archive_and_compact()<br/>把旧 active rows 标记为 compacted，仍可搜索\"]\n    C --> E[\"SessionDB active rows<br/>以后恢复并继续发给模型\"]\n```\n\n因此两件事可以同时成立：\n\n- 模型以后只看到压缩后的 active transcript；\n- 被压缩掉的旧消息仍保存在数据库中，并可通过搜索找回。\n\n## 5. `session_search` 怎样工作\n\n工具入口是 `tools/session_search_tool.py:L619`，底层查询是 `hermes_state.py:L4512` 的 `search_messages()`。\n\n```text\nsession_search(query)\n  → messages_fts 做词项搜索\n  → messages_fts_trigram 补充中文和子串匹配\n  → 返回 session、message id、时间和命中片段\n```\n\n搜索直接针对存储消息，不先让 LLM 总结。默认会包含 compacted rows，因此 context compression 不会让历史从搜索结果中消失；普通 rewind/undo 产生的 inactive、非 compacted rows 默认不返回。\n\n`session_search` 还支持：\n\n- 列出最近 session；\n- 读取指定 session 的 head/tail；\n- 围绕某个 message id 查看上下文窗口；\n- 在指定 profile 的 state.db 中搜索。\n\n## 6. 并发写入\n\nSessionDB 使用 SQLite WAL，并在写事务中使用 `BEGIN IMMEDIATE` 与短暂 jitter retry：`hermes_state.py:L1142–1194`。这解决 CLI、Gateway、后台 review 或子代理同时访问状态库时的常见锁竞争。\n\n## 7. 一句话总结\n\n```text\nmessages 是当前工作的会话事实\n  → 增量写入 SessionDB\n  → active rows 用于恢复模型上下文\n  → compacted rows 保留完整历史并继续支持搜索\n```\n", "available": true, "file": "05-会话存储与搜索.md", "rawEn": "# How Sessions Are Persisted and Searched\n\nInteractive sessions are stored in `$HERMES_HOME/state.db`. Four states matter:\n\n| State | Meaning |\n|---|---|\n| `conversation_history` | History at turn start and baseline for incremental writes |\n| `messages` | Canonical transcript being modified by the current loop |\n| `agent._session_messages` | Latest `messages` retained by a long-lived Agent |\n| SessionDB | Durable rows across processes and restarts |\n\n## 1. What SessionDB stores\n\nCore tables are defined at `hermes_state.py:L730–870`:\n\n```text\nstate.db\n├── sessions              metadata, system prompt, model, usage, status\n├── messages              user/assistant/tool, reasoning, calls/results\n├── messages_fts          regular full-text index\n├── messages_fts_trigram  CJK and substring index\n├── gateway_routing       platform-to-session routing\n└── compression_locks     cross-process compression locks\n```\n\nThe database keeps the full conversation, but not every row remains model context. Messages can be active, compacted, or inactive. Resume loads active rows; compacted history remains searchable.\n\n## 2. How one turn is written\n\n```mermaid\nflowchart TD\n    A[\"get_messages_as_conversation()<br/>Restore SessionDB active rows\"]\n    B[\"conversation_history<br/>Baseline for this turn's incremental writes\"]\n    C[\"build_turn_context()<br/>Shallow-copy history into mutable messages<br/>turn_context.py:L271–272\"]\n    D[\"Agent loop<br/>Append user, assistant, and tool results\"]\n    E[\"_flush_messages_to_session_db()<br/>Append only messages after the baseline<br/>run_agent.py:L1753\"]\n    F[\"agent._session_messages<br/>Keep the long-lived Agent's latest transcript\"]\n    G[\"finalize_turn()<br/>Persist again and close the turn correctly<br/>turn_finalizer.py:L185–207\"]\n    H[\"Next conversation_history<br/>Use live state or restore from DB\"]\n\n    A --> B --> C --> D --> E --> F --> G --> H\n```\n\n```text\nconversation_history = [old active messages]\nmessages = list(conversation_history)\nmessages += current user\nmessages += assistant(tool_calls)\nmessages += tool results\nmessages += final assistant\n\nagent._session_messages = messages\nSessionDB += messages not written yet\n```\n\n`_flush_messages_to_session_db()` (`run_agent.py:L1753–1910`) uses the baseline and persistence markers to append only new rows. The inbound user is also persisted early (`agent/turn_context.py:L335–347`) for crash recovery.\n\n## 3. Where the next turn resumes\n\n`get_messages_as_conversation()` (`hermes_state.py:L4084–4191`) reconstructs active rows, including reasoning and tool-call/result relationships.\n\n```text\nsame long-lived Agent\n  → continue from agent._session_messages\n\nrestart / resume\n  → SessionDB.get_messages_as_conversation()\n  → conversation_history\n  → build_turn_context()\n  → messages\n```\n\nThe stored system prompt is restored verbatim rather than rebuilt, preserving the cached prefix.\n\n## 4. What compression changes in the database\n\nIn-place compaction uses `archive_and_compact()` at `hermes_state.py:L3694–3744`:\n\n```text\nold active rows → active=0, compacted=1\nhead + handoff summary + recent tail → new active rows\nconversation_history → compressed messages baseline\n```\n\n```mermaid\nflowchart LR\n    A[\"Full active messages<br/>Transcript currently used by the model\"] --> B[\"ContextCompressor.compress()<br/>Replace the middle with a handoff summary\"]\n    B --> C[\"head + summary + tail<br/>New active transcript\"]\n    A --> D[\"archive_and_compact()<br/>Mark old active rows compacted and searchable\"]\n    C --> E[\"SessionDB active rows<br/>Resume and send these to the model\"]\n```\n\nThe model sees only the compressed active transcript, while older rows stay in the database for search.\n\n## 5. How `session_search` works\n\nThe tool entry is `tools/session_search_tool.py:L619`; the database query is `hermes_state.py:L4512`.\n\n```text\nsession_search(query)\n  → messages_fts for token search\n  → messages_fts_trigram for CJK and substrings\n  → session, message id, timestamp, matching snippet\n```\n\nSearch runs over stored messages without an LLM summary. Compacted rows are included by default; inactive non-compacted rewind/undo rows are excluded.\n\nThe tool can also list recent sessions, read a session's head/tail, inspect a window around a message id, and search another profile's database.\n\n## 6. Concurrent writes\n\nSessionDB uses SQLite WAL plus `BEGIN IMMEDIATE` and short jittered retries (`hermes_state.py:L1142–1194`) to handle common contention among CLI, Gateway, review, and subagent activity.\n\n## 7. Summary\n\n```text\nmessages is the current conversation truth\n  → incrementally persisted to SessionDB\n  → active rows restore model context\n  → compacted rows preserve searchable history\n```\n", "fileEn": "en/05-session-storage-search.md"}]};
